function onClick() {
  var sel = document.getElementById('option'),
    option = sel.value;     // options에는 선택한 옵션이 저장된다.

  var result = document.getElementById('result'), // result는 결과를 보여줄 곳
    input1 = document.getElementById('input1'),   // input1은 첫번째 숫자 값이 저장
    input2 = document.getElementById('input2');   // input2은 두번째 숫자 값이 저장

  /*
   * option값에 따라 적절한 함수를 호출하여 계산한 값을 result로 반환하라
   * hint: input의 값은 input.value를 호출하여 얻을 수 있다.
   * input.value는 string으로 반환되기 때문에 계산을 위해선 형 변환이 필요하다. (구글에 javascript type casting 혹은 자바스크립트 형변환 검색)
   *
   * result.innerHTML에 계산값을 넣으면 출력된다. (ex. result.innerHTML = 결과)
   */
  
   // 이곳에 코드를 작성하세요.
}

function addition(a, b) {
  // TO-DO
}

function subtraction(a, b) {
  // TO-DO
}

function multiplication(a, b) {
  // TO-DO
}

function division(a, b) {
  // TO-DO
}